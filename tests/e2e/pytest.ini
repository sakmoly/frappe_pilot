[pytest]
# Put tests/e2e on sys.path so specs can `import harness...` / `from flows...`.
pythonpath = .
addopts = -ra
markers =
    incremental: serial lifecycle steps — skip the rest of the module once one fails
